#include <stdio.h>
#include <string>

#define PBSTR "============================================================"
#define PBWIDTH 60

using namespace std;

void printProgress(double percentage, string message);